package net.minecraft;

import net.minecraft.nbt.ByteArrayTag;
import net.minecraft.nbt.ByteTag;
import net.minecraft.nbt.CompoundTag;
import net.minecraft.nbt.DoubleTag;
import net.minecraft.nbt.EndTag;
import net.minecraft.nbt.FloatTag;
import net.minecraft.nbt.IntArrayTag;
import net.minecraft.nbt.IntTag;
import net.minecraft.nbt.ListTag;
import net.minecraft.nbt.LongArrayTag;
import net.minecraft.nbt.LongTag;
import net.minecraft.nbt.ShortTag;
import net.minecraft.nbt.StringTag;

public interface class_5627 {
	void method_32302(StringTag stringTag);

	void method_32291(ByteTag byteTag);

	void method_32301(ShortTag shortTag);

	void method_32297(IntTag intTag);

	void method_32300(LongTag longTag);

	void method_32295(FloatTag floatTag);

	void method_32293(DoubleTag doubleTag);

	void method_32290(ByteArrayTag byteArrayTag);

	void method_32296(IntArrayTag intArrayTag);

	void method_32299(LongArrayTag longArrayTag);

	void method_32298(ListTag listTag);

	void method_32292(CompoundTag compoundTag);

	void method_32294(EndTag endTag);
}
